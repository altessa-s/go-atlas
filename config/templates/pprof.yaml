# Profiling configuration for the <name> service.
# Defines settings for Go's pprof profiling endpoints.
# All parameters are commented out by default (template mode).
# Uncomment and configure as needed for your environment.

# The pprof endpoints are exposed via the HTTP server, so ensure the HTTP server is
# configured if profiling is enabled. Profiling endpoints are available at: /debug/pprof/*

#pprof:
#  # <bool> Controls whether pprof endpoints are exposed.
#  # Set to true to enable profiling endpoints for performance analysis.
#  # When enabled, exposes various profiling endpoints for debugging and optimization.
#  # Default value: false
#  enable: false
