# factory

```go
import "github.com/altessa-s/go-atlas/data/leadelect/factory"
```

Package `factory` builds `leadelect.Leader` instances from configuration objects. Infrastructure references (NATS connection) are injected once at
construction.
