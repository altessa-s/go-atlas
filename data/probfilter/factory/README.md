# factory

```go
import "github.com/altessa-s/go-atlas/data/probfilter/factory"
```

Package `factory` builds probabilistic filter instances (Bloom, Cuckoo) and their storage backends from configuration objects.
