# factory

```go
import "github.com/altessa-s/go-atlas/data/locks/dlock/factory"
```

Package `factory` builds `dlock.DLock` instances from configuration objects. Infrastructure references (NATS connection) are injected once at
construction.
