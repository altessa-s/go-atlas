# redis

```go
import "github.com/altessa-s/go-atlas/data/uniq/providers/redis"
```

Package `redis` implements the unique value provider using Redis. Provides distributed unique key management across multiple service instances
with TTL support.
