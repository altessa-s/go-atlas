# nats

```go
import "github.com/altessa-s/go-atlas/data/uniq/providers/nats"
```

Package `nats` implements the unique value provider using NATS JetStream key-value store for distributed unique key management.
