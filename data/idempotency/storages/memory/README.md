# memory

```go
import "github.com/altessa-s/go-atlas/data/idempotency/storages/memory"
```

Package `memory` provides an in-memory implementation of idempotency storage with configurable TTL. Suitable for single-instance deployments,
tests, and local development.
