# kvstore

```go
import "github.com/altessa-s/go-atlas/data/mongo/cursor_storages/kvstore"
```

Package `kvstore` implements cursor storage using a generic key-value store interface for MongoDB pagination state persistence.
