# aws

```go
import "github.com/altessa-s/go-atlas/data/mongo/kms/aws"
```

Package `aws` implements the KMS provider interface using AWS Key Management Service for MongoDB CSFLE master key management.
