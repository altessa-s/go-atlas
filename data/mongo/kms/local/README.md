# local

```go
import "github.com/altessa-s/go-atlas/data/mongo/kms/local"
```

Package `local` implements the KMS provider interface using a local master key. Intended for development and testing only — not recommended for
production use.
