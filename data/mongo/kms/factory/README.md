# factory

```go
import "github.com/altessa-s/go-atlas/data/mongo/kms/factory"
```

Package `factory` builds KMS provider instances from configuration objects. Selects the appropriate provider (local, AWS, Azure, GCP) based on the
configuration.
