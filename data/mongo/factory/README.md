# factory

```go
import "github.com/altessa-s/go-atlas/data/mongo/factory"
```

Package `factory` builds `mongo.Mongo` client instances from configuration objects. Handles connection URI parsing, KMS setup, and encryption
model registration.
