# console

```go
import "github.com/altessa-s/go-atlas/observability/tracing/adapters/console"
```

Package `console` provides a console adapter for the Atlas tracing system. Outputs spans to stdout or stderr in human-readable or JSON
format. Designed for local development and debugging -- not recommended for production use.
