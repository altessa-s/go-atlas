# vault

```go
import "github.com/altessa-s/go-atlas/security/tlsutils/providers/vault"
```

Package `vault` provides a HashiCorp Vault PKI engine certificate provider. Generates certificates dynamically and supports automatic renewal.
