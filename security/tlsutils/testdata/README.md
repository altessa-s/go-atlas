# testdata

The `*.crt`, `*.key`, and `*.pem` files here are throwaway TLS fixtures produced by [`generate_certs.sh`](generate_certs.sh) for the
`tlsutils` test suite — self-signed, valid for no real host, and not real secrets. Safe to ignore or allowlist in secret scanners.
