# token

```go
import "github.com/altessa-s/go-atlas/security/vault/auth/token"
```

Package `token` provides direct token-based authentication for HashiCorp Vault. Validates and uses an existing Vault token without credential
exchange.
