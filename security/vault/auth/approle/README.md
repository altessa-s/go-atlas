# approle

```go
import "github.com/altessa-s/go-atlas/security/vault/auth/approle"
```

Package `approle` provides AppRole authentication for HashiCorp Vault. Designed for automated workflows where applications authenticate using
a role ID and secret ID pair.
