# gcp

```go
import "github.com/altessa-s/go-atlas/security/secrets/providers/gcp"
```

Package `gcp` provides a Google Cloud Secret Manager provider for the Atlas secrets system. Supports CRUD operations, concurrent retrieval,
base64 encoding, and label-based filtering.
